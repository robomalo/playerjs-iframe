import getScript from '../../../src/util/get-script';

describe('getScript()', () => {
  it('should available as function', () => {
    // TODO: better tests
    expect(getScript).to.be.a('function');
  });
});